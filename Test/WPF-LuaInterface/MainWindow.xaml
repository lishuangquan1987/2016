<Window x:Class="WPF_LuaInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <Grid>
        <Button Command="{Binding ClickCommand}" Content="Button" HorizontalAlignment="Left" Margin="180,150,0,0" VerticalAlignment="Top" Width="172" Height="36"/>
        <TextBox Text="{Binding Txt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="56" Margin="204,220,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="192"/>

    </Grid>
</Window>
